[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "github-actions-demo"
version = "0.1.0"
description = "A tiny package used by the GitHub Actions matrix demo."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]